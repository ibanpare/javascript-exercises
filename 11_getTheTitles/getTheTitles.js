const getTheTitles = function(arrayOfBooks) {
    const arrayOfTitles = arrayOfBooks.map(item => item.title);
    return arrayOfTitles;
};

// Do not edit below this line
module.exports = getTheTitles;
